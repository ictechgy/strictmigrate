// Marker build file: strictmigrate attributes `.kt` files to Gradle
// modules/source sets by directory convention, without running Gradle.
// (The demo never compiles Kotlin — measurement happens on the Swift side.)
kotlin("multiplatform") version "2.2.20"
